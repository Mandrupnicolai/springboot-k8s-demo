apiVersion: v1
kind: Secret
metadata:
  name: k8s-demo-secret
  namespace: default
type: Opaque
# Values must be base64-encoded.
# Example: echo -n "mypassword" | base64
stringData:
  DB_USER: "postgres"
  DB_PASSWORD: "changeme"   # Replace before deploying
