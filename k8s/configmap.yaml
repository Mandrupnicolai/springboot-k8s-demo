apiVersion: v1
kind: ConfigMap
metadata:
  name: k8s-demo-config
  namespace: default
data:
  APP_NAME: "springboot-k8s-demo"
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "taskdb"
  DB_POOL_SIZE: "10"
  LOG_LEVEL: "INFO"
  SERVER_PORT: "8080"
